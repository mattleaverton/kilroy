Input materialization contract:
- Consult `${{.InputsManifestPathEnvKey}}` for the complete resolved input set for this stage.
- The manifest path for this stage is `{{.ManifestPath}}`.
- Use `resolved_files` and `source_target_map[].target` as paths relative to cwd.
- Do not read `source` paths directly when they point outside cwd.
